package com.helpofai.videoplayer.feature.watch_party.analytics

class WatchPartyAnalyticsTracker {
    fun logSessionMetric(metricName: String, value: String) {
        // Collects playback performance metrics internally
    }
}
